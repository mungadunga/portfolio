import React from 'react';

// material
import { FC } from '../../material';
import './footer.css';

interface props {
   theme: string;
}

const Footer: FC<props> = (props) => {
   return (
      <p>hello world</p>
   )
}

export default Footer;